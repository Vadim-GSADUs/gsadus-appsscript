# Design Bundles — Dashboard & Pipeline Status / Handoff

> Snapshot copied from agent working-memory on **2026-06-25**. Living status of the
> Design Bundles data pipeline and the MoodBoardV1 dashboard, plus the open
> architecture decision for the WebCatalog/Supabase integration. Not pushed to
> Apps Script (`docs/**` + `**/*.md` are in `.claspignore`).

Revisiting (started 2026-06-24) the Design Bundles material-data system to centralize source of truth, harden data collection, and propagate downstream. WebCatalog is a potential consumer.

**Canonical store:** `GSADUs Materials.gsheet` → `Supplier` tab (one row per bundle×category). ETL is `AppsScript/GSADUs Materials/Code.js` (3 steps: pull links → sync assets → export `bundles_library.json`). Vault page: `Vault/wiki/curated/design-bundles.md`.

**The core gap:** Step 1 only auto-pulls `Supplier_URL` from the external Order Template (`1oGLgK...`, supplier-owned). `Supplier`/`Product_Name`/`Product_Size` are hand-typed — the one place provenance is undefined. Everything downstream is deterministic once those exist. Planned fix = an agentic "Step 1.5 enrichment" that web-extracts those fields from the supplier URL with a confidence/Needs_Review flag.

**Done (2026-06-24):**
- Created `AppsScript/GSADUs Materials/docs/SOURCES.md` — per-column provenance contract (step 1). Added `docs/**` + `**/*.md` to that repo's `.claspignore`. Commit `a52a70b`.
- **Legacy was already retired** (investigated, not a pending task): `gsadus_materials.db` no longer exists anywhere; `db_export.json` holds styles/environments only; PNGTools `prompt_engine.py` already loads bundles+hardware from `bundles_library.json`. The "cleanup" was just correcting stale docs that still described the old chain — fixed across Vault (`c93822c`) + PNGTools CLAUDE.md (`992108d`).
- Vault governance policy redesigned (separate subagent): `maintained:` is now a cadence hint, agents fix facts anywhere, `owner: human` is the only narrow content lock. Vault commit `f10f09f`. Follow-up: flip `workspaces.md`/`workspace-dashboard.md` to `maintained: agent` after user commits their pending edits.

**Data state (2026-06-24):** collection is essentially complete/clean — 30 unique products / 36 rows, only the 6 Cabinet_Color rows lack size/url (correctly — cabinet finishes have no tile dims). Agentic enrichment deprioritized (no real dirt to backfill).

**Schema 1.2 — images as a separate tab:**
- Decision: keep material data flat (mild 1.2x reuse); split images into a new `Images` tab (1-to-many, type-attributed). No in-sheet collector (JSON is the join). Canonical image format = PNG via PNGTools. Full design in `AppsScript/GSADUs Materials/docs/SOURCES.md` §7.
- `Code.js` rewritten (commit `5e922f8`): new `Images` tab + menu (`Seed Images from Supplier`, `Sync Image Assets`); `exportToJson` → schema 1.2 nesting `images[]`, drops material-level image fields.
- PNGTools consumer updated (commit `54cd863`): `core/darkroom/bundle_references.py` reads `Material_Image` from `images[]`. Tests green.

**Live deploy confirmed (2026-06-25):** `clasp push` of the bound `Code.js` = "already up to date" — schema 1.2 is live on the canonical sheet (`1JT5NJED-NiqOIuC6b-tq78e5mXDUonD-R7EAvaaZriM`, shared drive). Live DB has Supplier + Images + Hardware tabs; Images populated with canonical PNGs. Cleanup confirmed 2026-06-25: GWS `spreadsheets.batchUpdate` deleted Supplier columns I:Z, removing the stale legacy image cols (`File_ID`, `Drive_URL`, `Filename`, `Sync_Status`) plus empty placeholder columns. Supplier now ends at `HScale`; canonical image data lives only in Images.

**MoodBoardV1 — standalone read-only HTML dashboard (2026-06-25):** migrated the "Design Bundles" standalone Apps Script (`1ZC3oIdEAF-OM0tVWs6E8ocNOI9QYpiLZ6sgi4Ef6ZwBdQZ9po9OILbFn`, owned vadim@gsadus.com) from the experimental Test DB (`1LRjCfGaKlDEuUjAACHb32AqXZNZSHOJ2L4_qiMNP6AQ`, old pre-Images schema) → canonical DB. New clasp project at `AppsScript/GSADUs Materials/MoodBoardV1` (manual `.clasp.json`; clasp clone refused because parent bound-script `.clasp.json` is up-tree). Added `MoodBoardV1/**` to parent `.claspignore`. Server `Code.js` rewritten read-only: reads Supplier ⋈ Images by `Material_Key = canonicalBasename(Supplier, Product_Name)`, projects to the lowercased headers the HTML resolves by, sources image/file_id/drive_url from Images (canonical), extracts real Supplier_URL from the =HYPERLINK formula. Dropped all CRUD (update/insert/delete) + the HTML's edit/add/delete UI. Join verified 36/36 materials. `index.html.html` filename left as-is so clasp name round-trips and `createHtmlOutputFromFile('index')` resolves. **Deployment gotcha:** the script had 2 deployments — `@HEAD` (always latest) + a versioned one (`AKfycbxYJkXjXr2XqCP_lF7KEFgLAmwacFQgm_ZLIQaTqn5j-1RphctR2fyqEaL3bfIybu0p`). `clasp push` only updates HEAD, so the versioned `/exec` URL kept serving the OLD test-DB code until `clasp deploy --deploymentId <id>` bumped it to a new version. Web-app URL = `https://script.google.com/macros/s/AKfycbxYJkXjXr2XqCP_lF7KEFgLAmwacFQgm_ZLIQaTqn5j-1RphctR2fyqEaL3bfIybu0p/exec`. Added withFailureHandler + a no-backend message (the inline editor "Launch preview" always spins because google.script.run is undefined there — not a bug; must test via /exec). Architecture note: the **standalone** script is the web app (has doGet); the canonical sheet's bound GSADUs Tools script is container-bound (ETL menu only) and must NOT be web-app-deployed. Distinct from the existing `Moodboard/` subfolder = a Google **Slides** mood board (bound to a .gslides, reads File_ID off Supplier; already points at the current DB).

**MoodBoardV1 dashboard iterations (2026-06-25):** read-only, image-first redesign — removed all write UI; board is now a uniform 3-col grid of large `aspect-square` swatches with inline captions (category·name·supplier·size), no pill badges; cut the "Audit System" (health) tab and all overlay/redundant-label slop. Added a **sample-data fallback** (renders layout in the backend-less editor preview with a "Preview · sample data" badge). Added an **"Installed Showcase" panel** below the grid showing the selected material's `Showcase_Image` (server join now returns `{material, showcase}` per Material_Key; emits a `showcase` thumbnail field). **Gap:** no DB has Showcase_Image rows yet AND bound `seedImagesFromSupplier` only seeds Material_Image — so showcases are hand-added until a "Seed Showcase" step exists; dashboard shows an empty-state prompting exactly that. Dashboard currently shows 1 showcase/material (first wins); multi-image gallery = future. Bound GSADUs Tools menu got an "Open Mood Board" launcher (opens the web-app /exec in a new tab). All UI pushed + live: versioned `/exec` redeployed to **@4** (2026-06-25 wrap-up). Fine-tuning PAUSED — pivoting to the WebCatalog/Supabase/GCS integration next (see PROPOSED direction below), which supersedes adding write logic to GAS.

**PROPOSED next direction — make the dashboard read-WRITE (discussed 2026-06-25, not started):** vision = curate the non-supplier data (Material Images, Showcase Images, scales, notes, some specs) via the dashboard with upload + sync back to the Materials Drive folder. Key architectural calls to make BEFORE coding:
1. **Canonicalization authority** — PNGTools owns PNG normalization; dashboard must NOT transcode or create a 2nd canonical path (workspace rule #6). Pattern: dashboard captures intent (raw upload + pending Images row) → existing bound `syncImageAssets`/PNGTools normalizes (rename `{Material_Key}__{type}[-n].png`, place in Materials folder, read dims).
2. **GAS feasibility** — spec-edits (VScale/HScale/Notes via google.script.run→setValue) are easy/low-risk = good first step; **image upload is the friction** (google.script.run payload limits, no transcoding in GAS, `executeAs USER_DEPLOYING` = everyone edits as Vadim w/ no attribution).
3. **Strategic fork** — Path A extend GAS dashboard (cheap, hits GAS media/auth ceiling) vs Path B build write/upload against **WebCatalog's Supabase+GCS stack** with an explicit app host decision (the current WebCatalog repo is pipeline/schema/docs, not an active Next.js app). **User's call (2026-06-25): go Path B — push Materials data + media into Supabase + GCS as the canonical store rather than growing parallel GAS write logic.** Needs a thorough dedicated plan next session.

**Open questions for the WebCatalog/Supabase plan:** does Supabase become the source of truth or mirror the sheet? · editable-field set · upload target (Materials folder vs staging) · identity/auth model · how PNGTools canonicalization fits the new storage (GCS).

**Other next candidates:** propagation (GAP-05/INT-02 Revit/Architextures material sync) — was the chosen frontier before this schema work; recurring-sync hardening.
